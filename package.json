{
  "name": "rwserve-brute-force",
  "version": "1.0.2",
  "description": "RWSERVE plugin to temporarily blacklist an IP address that attempts to POST too many times in a given period",
  "private": true,
  "author": "Read Write Tools",
  "license": "MIT License",
  "main": "dist/index.js",
  "scripts": {
    "bump-version": "npm version patch && git push && git push --tags",
    "local": "node ../../app/rwserve/dbg/main.js ./etc/brute-force-config",
    "start": "rwserve ./etc/brute-force-config"
  },
  "dependencies": {
    "rwserve-plugin-sdk": "git+ssh://root@nk024:/keep/rwserve-plugin-sdk#"
  },
  "files": [
    "dist",
    "etc",
    "img"
  ]
}
